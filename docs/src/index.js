import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
//import Weather from './Weather';

// ReactDOM.render(<Weather />, document.getElementById('weather-root'));

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

